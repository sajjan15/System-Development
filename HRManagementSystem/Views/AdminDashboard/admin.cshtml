<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>HR Management System</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #ffffff;
           
            background-size: cover;
            background-position: center;
        }

        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            background-color: rgba(255, 255, 255, 0.9); 
            color: #333;
            padding: 15px 30px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

            .header h1 {
                margin: 0;
                font-size: 24px;
                font-weight: bold;
                color: #007bff;
            }

        .user-info {
            position: relative;
        }

            .user-info button {
                background-color: rgba(255, 255, 255, 0.8); 
                color: #007bff;
                border: 1px solid #007bff;
                cursor: pointer;
                padding: 10px 15px;
                font-size: 16px;
                border-radius: 5px;
                transition: all 0.3s ease;
            }

                .user-info button:hover {
                    background-color: rgba(255, 255, 255, 0.6); 
                }

            .user-info .dropdown {
                display: none;
                position: absolute;
                right: 0;
                top: 100%;
                background-color: rgba(255, 255, 255, 0.9);
                box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
                min-width: 150px;
                border-radius: 5px;
                overflow: hidden;
                z-index: 10;
            }

                .user-info .dropdown a {
                    color: #333;
                    padding: 10px 15px;
                    text-decoration: none;
                    display: block;
                    transition: all 0.3s ease;
                }

                    .user-info .dropdown a:hover {
                        background-color: rgba(0, 123, 255, 0.1); 
                    }

            .user-info:hover .dropdown {
                display: block;
            }

        .main-content {
            padding: 40px 20px;
            text-align: center;
        }

            .main-content button {
                background-color: rgba(255, 255, 255, 0.9); 
                color: #007bff;
                border: 1px solid #007bff;
                padding: 15px 30px;
                margin: 10px;
                font-size: 16px;
                cursor: pointer;
                border-radius: 5px;
                transition: all 0.3s ease;
                box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            }

                .main-content button:hover {
                    background-color: rgba(255, 255, 255, 0.7); 
                    transform: translateY(-2px);
                    box-shadow: 0 6px 12px rgba(0, 0, 0, 0.15);
                }

                .main-content button:active {
                    transform: translateY(0);
                    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
                }
    </style>
</head>
<body>
    <div class="header">
        <h1>HR Management System</h1>
        <div class="user-info">
            <button>
                Logged in as @Context.Session.GetString("LoggedInUser") ▼
            </button>
            <div class="dropdown">
                <a href="#" onclick=" profile()">Profile</a>
                <a href="#">Settings</a>
                <a href="#" onclick="logout()">Logout</a>
            </div>
        </div>
    </div>

    <div class="main-content">
        <button onclick="addEmployee()">Add Employee</button>
        <button onclick="addDeposit()">Add Deposit</button>
    </div>

    <script>
        function addEmployee() {
            // Navigate to the Add Employee page
            window.location.href = '/AdminDashboard/AddEmployee';
        }

        function addDeposit() {
            // Navigate to the Add Deposit page
            window.location.href = '/AdminDashboard/AddDeposit';
        }

        function profile() {
            // Navigate to the Profile page
            window.location.href = '/ProfileDashboard/Profile';
        }

        function logout() {
            alert('Logging out...');
            window.location.href = '/Login/Login';
        }
    </script>
</body>
</html>
